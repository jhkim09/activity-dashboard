services:
  - type: web
    name: activity-dashboard
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TALLY_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
