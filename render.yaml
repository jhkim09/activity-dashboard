services:
  - type: web
    name: activity-dashboard
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: kanban-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: TALLY_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
